<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.allxone.mybatisprojectbackend.mapper.TaxInformationMapper">
    <resultMap id="taxInformationResultMap" type="com.allxone.mybatisprojectbackend.model.TaxInformation">
        <id property="id" column="id"/>
        <result property="employeeId" column="employee_id"/>
        <result property="taxRate" column="tax_rate"/>
        <result property="taxExemption" column="tax_exemption"/>
        <result property="createdAt" column="created_at" javaType="java.time.Instant"/>
        <result property="updatedAt" column="updated_at" javaType="java.time.Instant"/>
    </resultMap>

    <insert id="saveTaxInformation" parameterType="TaxInformation" useGeneratedKeys="true"
            keyProperty="id">
        INSERT INTO tax_information (id, employee_id, tax_rate, tax_exemption, created_at, updated_at)
        VALUES (#{id}, #{employeeId}, #{taxRate}, #{taxExemption}, NOW(), NOW())
    </insert>

    <select id="getTaxInformationById" parameterType="int" resultMap="taxInformationResultMap">
        SELECT * FROM tax_information WHERE id = #{id}
    </select>

    <select id="getAllTaxInformation" resultMap="taxInformationResultMap">
        SELECT * FROM tax_information
    </select>

    <select id="getTaxInformationByEmployeeId" parameterType="Long" resultMap="taxInformationResultMap">
        SELECT * FROM tax_information WHERE employee_id = #{id}
    </select>

    <update id="updatePayroll" parameterType="Payroll">
        UPDATE payroll
        <set>
            <if test="employeeId != null">
                employee_id = #{employeeId},
            </if>
            <if test="taxRate != null">
                tax_rate = #{taxRate},
            </if>
            <if test="bonus != null">
                tax_exemption = #{taxExemption},
            </if>
            updated_at = NOW()
        </set>
        WHERE id = #{id}
    </update>

    <delete id="deleteTaxInformationById" parameterType="int">
        DELETE FROM tax_information WHERE id = #{id}
    </delete>
</mapper>
