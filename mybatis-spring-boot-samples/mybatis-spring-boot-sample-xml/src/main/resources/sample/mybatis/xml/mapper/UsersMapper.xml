<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="sample.mybatis.xml.mapper.UsersMapper">
    <select id="selectUserByUserName" resultType="Users">
        select username, name, gender, address, age, is_active, email, phone_number from users where username = #{value}
    </select>

    <update id="updateUser" parameterType="Users">
        UPDATE users SET user_name = #{userName}, user_pass = #{userPass} WHERE user_id = #{userId}
    </update>

    <select id="selectUserByEmail" resultType="Users">
        select username, name, gender, address, age, is_active, email, phone_number from users where email = #{value}
    </select>

    <insert id="createUser" parameterType="Users">
        INSERT INTO users (username, email, password, name, role_id) VALUES (#{userName}, #{email}, #{password}, #{name}, #{roleId})
    </insert>

    <select id="getAllUsers" resultType="Users">
        SELECT * FROM users
    </select>

    <update id="setAdmin" parameterType="sample.mybatis.xml.domain.Users">
        UPDATE users SET is_admin = #{isAdmin} WHERE user_id = #{userId}
    </update>
</mapper>