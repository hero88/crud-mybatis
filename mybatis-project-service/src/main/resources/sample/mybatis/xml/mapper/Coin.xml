<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.allxone.mybatisprojectservice.mapper.Coin">
    <select id="findAllCoin" resultMap="coinResultMap">
        select * from coins
    </select>

    <select id="getAllCoinsByUserId" parameterType="long" resultMap="coins">
        SELECT *
        FROM coins
        WHERE user_id = #{userId};
    </select>

    <select id="selectCoinById" parameterType="long" resultMap="coins">
        SELECT *
        FROM coins
        WHERE id = #{id};
    </select>
    <resultMap id="coins" type="com.allxone.mybatisprojectservice.model.Coin">
        <id property="id" column="id"/>
        <result property="name" column="name"/>
        <result property="symbol" column="symbol"/>
        <result property="coinmarketId" column="coinmarket_id"/>
        <result property="quantity" column="quantity"/>
        <result property="userId" column="user_id"/>
        <result property="createdAt" column="created_at"/>
        <result property="updatedAt" column="updated_at"/>
        <!-- Thêm các trường khác nếu cần -->
    </resultMap>
</mapper>